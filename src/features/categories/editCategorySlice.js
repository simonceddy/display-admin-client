import { createSlice } from '@reduxjs/toolkit';

export const editCategorySlice = createSlice({
  name: 'editCategory',
  initialState: {},
  reducers: {

  },
});

// export const { } = editCategorySlice.actions;

export default editCategorySlice.reducer;
